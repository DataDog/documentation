{
    "name": "hugo-markdoc-integration",
    "version": "1.0.0",
    "description": "",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build-markdoc-integration": "rimraf ./dist && npx tsc && cp ./src/variables.config.yaml ./dist/variables.config.yaml",
        "build-prod-renderer": "npx esbuild ./src/barkdoc-client-renderer.js --bundle --minify > ./dist/barkdoc-client-renderer.min.js",
        "build-dev-renderer": "npx esbuild ./src/barkdoc-client-renderer.js --bundle > ./dist/barkdoc-client-renderer.js",
        "build": "npm run build-markdoc-integration && npm run build-dev-renderer && npm run build-prod-renderer"
    },
    "dependencies": {
        "js-yaml": "^4.1.0",
        "rimraf": "^5.0.5",
        "barkdoc": "file:../barkdoc",
        "zod": "^3.22.4"
    },
    "devDependencies": {
        "@types/js-yaml": "^4.0.9",
        "@types/node": "^20.11.30",
        "typescript": "^5.4.3",
        "esbuild": "0.20.2"
    }
}
