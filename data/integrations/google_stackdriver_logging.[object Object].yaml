google_stackdriver_logging:
  - description: Nombre d'octets dans toutes les logs ingérées.
    integration: google_stackdriver_logging
    metric_name: gcp.logging.byte_count
    metric_type: count
    orientation: '0'
    short_name: log bytes
    unit_name: byte
  - description: >-
      Nombre de logs qui n'ont pas contribué aux métriques définies par
      l'utilisateur.
    integration: google_stackdriver_logging
    metric_name: gcp.logging.dropped_log_entry_count
    metric_type: count
    orientation: '-1'
    short_name: log entry drops
    unit_name: entry
  - description: Nombre de logs qui ont contribué aux métriques définies par l'utilisateur.
    integration: google_stackdriver_logging
    metric_name: gcp.logging.log_entry_count
    metric_type: count
    orientation: '0'
    short_name: log entries
    unit_name: entry
