---
aliases:
- /kr/events/stream/
further_reading:
- link: /api/v1/events/
  tag: 설명서
  text: Datadog 이벤트 API
- link: https://www.datadoghq.com/blog/datadog-events/
  tag: 블로그
  text: 개선된 Datadog 이벤트로 더 빠르게 문제 해결
kind: 설명서
title: 이벤트 익스플로러
---

{{< img src="events/explorer/events-overview.png" alt="이벤트 익스플로러" style="width:100%;" >}}

이벤트 익스플로러는 코드 배포, 서비스 상태, 설정 변경 또는 모니터링 경고와 같은 인프라 및 서비스에서 생성된 가장 최근 이벤트를 표시합니다.

## 익스플로러로 이동

{{< img src="events/explorer/navigate.png" alt="이벤트 익스플로러 이동 방법" style="width:100%;" >}}

이벤트 익스플로러는 기본적으로 모든 이벤트 유형을 표시합니다. 패싯 또는 검색 쿼리별로 이벤트를 필터링할 수 있습니다.

### 이벤트 사이드 패널

{{< img src="events/explorer/side-panel.mp4" alt="이벤트 익스플로러의 열을 클릭하면 이벤트 사이드 패널이 열립니다" style="width:100%;" video=true >}}

이벤트 익스플로러의 열을 클릭하면 이벤트 사이드 패널이 열립니다. 여기에는 이벤트와 관계된 태그와 메시지가 표시됩니다.

### 속성 탭

{{< img src="events/explorer/event-attribute.png" alt="이벤트 속성 탭 예시" style="width:100%;" >}}

사이드 패널의 속성 탭에는 이벤트 속성이 JSON으로 나열됩니다. 속성을 클릭하여 기존 쿼리에 추가하거나, 기존 쿼리에서 제외하거나, 이 속성의 열을 추가합니다.

이벤트 속성은 프로세싱 파이프라인을 사용하여 획득 시 정규화하거나 강화할 수 있습니다.

## 옵션

이벤트 익스플로러를 커스텀하려면 **옵션**을 클릭합니다.

- 표시되는 열을 조정합니다.
- 축소에서 확장으로 목록 밀도를 선택합니다.
- 절대 또는 상대 타임스탬프를 표시합니다. 절대 타임스탬프는 이벤트 시간을 UTC로 표시합니다(예: `Aug 11 15:58.08.000`). 상대 타임스탬프는 이벤트 이후 경과된 시간을 표시합니다(예: `20 seconds ago`).
- 결과에서 태그를 표시하거나 숨깁니다.
- 상단 타임라인 그래프를 표시하거나 숨깁니다.

## 시간 범위

시간 프레임 선택기는 익스플로러 또는 애널리틱스에 표시되는 이벤트 시간 범위를 관리하고, 검색 표시줄 아래의 타임라인 범위를 설정합니다.

시간 프레임 드롭다운에서 프리셋 범위를 선택하거나 커스텀 시간 범위를 입력하여 시간 범위를 변경합니다.

모든 검색 파라미터가 URL 내에 포함되어 있으므로 URL을 공유하여 보기를 공유할 수 있습니다.

## 검색 구문

이벤트 검색은 [로그 검색 구문][1]을 사용합니다.

로그 검색과 마찬가지로 이벤트 검색은 다음을 허용합니다.

- `AND`, `OR` 및 `-` 연산자
- 와일드카드
- 이스케이프 문자
- `key:value`가 있는 태그 및 패싯 검색
- `@` 접두사가 있는 속성 내 검색

쿼리 예시:

`source:(github OR chef)`
: GitHub OR Chef의 이벤트를 표시합니다.

`host:(i-0ade23e6 AND db.myapp.com)`
: `i-0ade23e6` AND `db.myapp.com`의 이벤트를 표시합니다.

`service:kafka`
: `kafka` 서비스의 이벤트를 표시합니다.

`status:error`
: `error` 상태의 이벤트를 표시합니다(지원 대상: `error`, `warning`, `info`, `ok`).

`availability-zone:us-east-1a`
: `us-east-1a` AWS 사용 가능 영역(AZ)의 이벤트를 표시합니다.

`container_id:foo*`
: `foo`로 시작하는 ID가 있는 모든 컨테이너의 이벤트를 표시합니다.

`@evt.name:foo`
: `evt.name` 속성이 `foo`와 동일한 이벤트를 표시합니다.

자세한 내용은 [로그 검색 구문][1]을 참조하세요.

### 패싯

Datadog은 이벤트 속성을 이벤트 익스플로어 패싯 사이드 패널, 애널리틱스 및 모니터에서 액세스할 수 있는 패싯 또는 측정값으로 인덱싱합니다.

패싯은 속성 또는 태그의 고유 구성 요소를 표시하고, 표시되는 이벤트의 수와 같은 기본 애널리틱스 결과를 제공합니다. 패싯을 사용하면 주어진 속성을 기반으로 데이터세트를 피벗하거나 필터링할 수 있습니다. 필터링하려면 보려는 값을 선택하세요.

{{< img src="events/explorer/facets-location.png" alt="패싯 사이드 패널" style="width:100%;" >}}

### 패싯 생성

패싯을 생성하려면 왼쪽 패널에서 “패싯 추가” 버튼을 사용합니다.

패싯을 추가하면 해당 속성의 값이 모든 새로운 보기에 저장되고, 검색 표시줄 및 패싯 사이드 패널에서 사용할 수 있습니다. 이벤트 모니터 및 그래프 위젯에서 그룹화할 때에도 사용할 수 있습니다.

### 예약 속성

이 목록은 이벤트와 함께 자동으로 수집된 예약 속성을 설명합니다.

| 속성 | 설명                                                                                                                                                                                                                                |
|-----------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `host`    | 메트릭에 정의된 원래 호스트의 이름입니다. Datadog은 Datadog 내 일치하는 호스트에서 해당 호스트 태그를 자동으로 검색하여 이벤트에 적용합니다. 에이전트는 이 값을 자동으로 설정합니다.                          |
| `source`  | 통합 이름 또는 이벤트가 시작된 기술에 해당합니다. 통합 이름과 일치하면 Datadog은 해당 파서 및 패싯을 자동으로 설치합니다. 그 예로는 `nginx`, `postgresql` 등이 있습니다. |
| `status`  | 이벤트의 수준 또는 중요도에 해당합니다.      |
| `service` | 이벤트를 생성하는 애플리케이션 또는 서비스의 이름입니다. |
| `message` | 기본적으로 Datadog은 `message` 속성 값을 이벤트 항목의 본문으로 수집합니다. |   

예약 속성과 동일한 키를 가진 태그를 검색하려면 `tags` 검색 구문을 사용하세요. 
그 예로는 `tags:("status:<status>")`이 있습니다.

예약 속성과 동일한 키가 있는 태그에 패싯을 생성하는 방법은 다음과 같습니다.
1. [리매퍼 프로세서][2]를 사용하여 태그를 다른 태그나 속성에 다시 매핑합니다.
2. 새 태그/속성에 [패싯][3]을 생성합니다.

## 알림

Datadog은 API의 포스팅 시 이벤트 메시지에서 `@notifications`를 지원합니다. 예를 들면 다음과 같습니다.

`@all`
: 사용자 조직의 모든 구성원에게 알림을 보냅니다.

`@test@example.com`
: `test@example.com`으로 이메일을 보냅니다.

`@slack-<SLACK_ACCOUNT>-<CHANNEL_NAME>`
: 지정된 Slack 채널에 이벤트 또는 그래프를 게시합니다.

`@webhook`
: 웹훅을 경고하거나 트리거합니다. [웹훅의 블로그 게시물][4]을 참조하세요.

자세한 내용은 [알림][5]을 참조하세요.

## 이벤트 애널리틱스

이벤트 애널리틱스는 트러블슈팅 및 모니터링을 위한 보기, 데이터 집계 및 그룹화 기능으로 이벤트 익스플로러 페이지를 확장합니다. 사용자는 다음을 관리할 수 있습니다.

- 분석할 보기 세트를 필터링하는 쿼리.
- 데이터를 그룹화할 범위.
- 집계 및 그룹에 대한 시각화 방법.

애널리틱스 시각화 기능을 내보내 대시보드 또는 노트북에서 위젯을 생성할 수 있습니다.

### 애널리틱스 쿼리 빌드

쿼리를 사용하여 이벤트 애널리틱스에 표시되는 내용을 관리합니다.

1. 그래프화할 속성 또는 태그를 선택한 뒤 이를 패싯으로 추가합니다. 패싯을 그래프화하면 변수의 고유 카운트 값이 표시됩니다.
    {{< img src="events/explorer/facet-to-graph.png" alt="그래프화할 수 있는 패싯 목록을 표시합니다." style="width:100%;" >}}
2. 패싯을 사용하여 그래프를 그룹화합니다. 여기에서 속성을 사용하려면 속성을 패싯으로 추가해야 합니다.
    {{< img src="events/explorer/split-graph.png" alt="데이터별로 그룹화할 수 있는 패싯 목록을 표시합니다." style="width:100%;" >}}
3. 그래프의 시간 간격을 선택합니다. 글로벌 시간 프레임을 변경하면 사용 가능한 타임스텝 값 목록이 변경됩니다. 그 결과를 시계열, 테이블 또는 상위 목록으로 표시할 수 있습니다.
    {{< img src="events/explorer/time-interval.png" alt="기본값인 5초를 포함하여 사용 가능한 시간 간격 목록을 표시합니다." style="width:100%;" >}}
4. 선택된 측정값에 따라 상위 또는 하위 값을 표시하도록 선택합니다.
    {{< img src="events/explorer/display-values.png" alt="값을 상단에 표시할지 하단에 표시할지 선택합니다." style="width:100%;" >}}

## 참고 자료

{{< partial name="whats-next/whats-next.html" >}}


[1]: /kr/logs/explorer/search_syntax/
[2]: https://docs.datadoghq.com/kr/logs/log_configuration/processors/?tab=ui#remapper
[3]: https://docs.datadoghq.com/kr/events/explorer/#create-a-facet
[4]: https://www.datadoghq.com/blog/send-alerts-sms-customizable-webhooks-twilio
[5]: /kr/monitors/notify/