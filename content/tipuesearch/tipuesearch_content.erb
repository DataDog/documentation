<%
require 'nokogiri'
pages = @items.select{ |item| ((%w{article page documentation integration guide}.include? item[:kind]) && !(%w{ja}.include? item[:language])) }.collect{ |item|
  {
    "title" => item[:title],
    "text" => Nokogiri::HTML(item.compiled_content).at('body').inner_text,
    "tags" => "",
    "loc" => item.rep_named(:default).path
  }
}
%>
var tipuesearch = {"pages": <%= Oj.dump(pages) %>};
