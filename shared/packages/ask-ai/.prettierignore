# Prettier does not read .gitignore (it only auto-skips node_modules), so the
# generated and vendored trees have to be listed again here.
node_modules/
dist/

# The committed Yarn release is a 2.9 MB minified bundle. It ships its own
# `//prettier-ignore` header, but listing it here keeps Prettier from reading
# the file at all.
.yarn/
