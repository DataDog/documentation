Throwaway marker for testing the dynamic-build preview gate
(documentation-ci FF_DYNAMIC_BUILD_PREVIEW, PR #231).

Exists only to give the branch a commit so a pipeline can run. Delete this
branch and this file once the gate has been validated.
