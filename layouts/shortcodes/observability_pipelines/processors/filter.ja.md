このプロセッサーは、指定されたフィルタークエリに一致するログをフィルタリングし、一致しないログをすべて破棄します。このプロセッサーでログが破棄された場合、このプロセッサーより下位のプロセッサーはそのログを受信しません。このプロセッサーは、デバッグログや警告ログなどの不要なログをフィルタリングできます。

Filter プロセッサーをセットアップするには、次の手順に従います。
- **フィルタークエリ**を定義します。指定した[クエリ](#filter-query-syntax)は、そのクエリに一致するログのみをフィルタリングして通過させ、他のログはすべて破棄します。