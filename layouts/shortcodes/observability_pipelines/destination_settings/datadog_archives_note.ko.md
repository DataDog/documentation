<div class="alert alert-danger">Worker가 Datadog Agent에서 들어오지 않는 로그를 수집하고, 해당 로그가 Observability Pipelines의 Datadog Archives 대상을 통해 아카이브로 전송되는 경우, 해당 로그에는 <a href="https://docs.datadoghq.com/logs/log_configuration/attributes_naming_convention/#reserved-attributes">예약된 속성</a>이 태그되지 않습니다. 또한, Datadog으로 Rehydrate한 로그에는 표준 속성이 매핑되지 않습니다. 즉, 로그를 Log Management로 복원할 때 로그를 아카이브로 라우팅하기 전 Observability Pipelines에서 로그를 적절히 설정 및 재매핑하지 않으면 Datadog 텔레메트리, 로그 검색 기능, <a href="https://docs.datadoghq.com/getting_started/tagging/unified_service_tagging/?tab=kubernetes">통합 서비스 태깅</a>의 이점을 잃을 수 있습니다.<br><br>예를 들어, syslogs가 Datadog Archives로 전송되고 해당 로그의 <code>상태</code>는 예약된 속성 <code>상태</code>가 아니라 <code>severity</code>로 태그되어 있는 상태를 가정해 보겠습니다. 호스트는 예약된 속성 <code>hostname</code>이 아닌 <code>host-name</code>으로 태그되어 있습니다. 해당 로그가 Datadog으로 Rehydrate되면, 각 로그의 상태는 <code>info</code>로 설정되고, 어느 로그에도 hostname 태그가 포함되지 않습니다.
</div>