<div class="alert alert-info">
<p>You can enable App and API Protection for Node.js services with the following setup options:</p>

<ol>
<li>If your Node.js service already has APM tracing set up and running, then skip to <a href="#automatically-enabling-app-and-api-protection-through-remote-configuration">service configuration</a></li>
<li>If your Node.js service doesn't have APM tracing set up, you can easily enable App and API Protection with Datadog's <a href="https://docs.datadoghq.com/tracing/trace_collection/automatic_instrumentation/single-step-apm/{{ .Get "platform" }}/">Automatic Installation</a></li>
<li>Otherwise, keep reading the following manual setup instructions</li>
</ol>
</div> 