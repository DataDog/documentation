Automatically create a case for every new security signal. By default, the **Cases** section lists all projects that have [project handles][301]. You can click **View All**, then click **+** to add a handle to an existing project and enable automatic case creation for it.

[301]: /incident_response/case_management/settings#monitors