---
title: "Test Log Processor API Shortcode"
processor_type: "CreateLogsPipeline"
---

# Test Log Processor API Shortcode

This is a test page to demonstrate the new `log-processor-api` shortcode.

## API Documentation

The following shows the API documentation for the CreateLogsPipeline processor:

{{< log-processor-api >}}

## Usage Notes

- The shortcode automatically detects the processor type from the `processor_type` parameter
- It displays both the schema table (Model tab) and JSON example (Example tab)
- The same CSS fix we implemented earlier prevents the dash issue

---

**Note:** This is a test file and should be removed after testing.
