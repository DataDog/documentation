## Sampling

Use `setSessionSampleRate(<sampleRate>)` to set the RUM sessions sample rate. A value of `0` means no RUM events are sent, while `100` means all sessions are kept. For the full parameter reference, see [Initialization parameters][1].

[1]: /real_user_monitoring/setup/enable_rum/advanced_configuration/?platform=kotlin_multiplatform#initialization-parameters
