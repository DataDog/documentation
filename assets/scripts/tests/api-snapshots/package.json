{
  "name": "api-snapshots",
  "version": "0.0.0",
  "private": true,
  "description": "Throwaway harness for shorter-api-pages migration. Diffs endpoint HTML before/after the per-endpoint page split. Delete when migration lands.",
  "scripts": {
    "test": "vitest run",
    "test:update": "vitest run -u"
  },
  "devDependencies": {
    "cheerio": "^1.0.0",
    "vitest": "^3.2.0"
  }
}
