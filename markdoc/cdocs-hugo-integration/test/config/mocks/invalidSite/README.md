# Invalid mock site

Every Markdoc (`.mdoc`) file in the content folder of this site contains a mistake. Each file is named after the error made in it, and the bad markup itself should have the literal word "mistake" somewhere near it.

Each file is verified by the tests to cause an error.